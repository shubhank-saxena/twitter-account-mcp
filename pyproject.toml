[project]
name = "twitter-account-mcp"
version = "0.1.0"
description = "MCP server for accessing Twitter/X account"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
    "twikit>=2.3.3",
]
